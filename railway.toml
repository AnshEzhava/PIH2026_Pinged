[build]
builder = "nixpacks"
buildCommand = "cd backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && python prewarm.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
